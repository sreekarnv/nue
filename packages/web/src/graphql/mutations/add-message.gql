mutation AddMessage($receiver: String!, $text: String!) {
	addMessage(receiver: $receiver, text: $text) {
		_id
		sender {
			_id
			name
		}
		receiver {
			_id
			name
		}
		text
		createdAt
		__typename
	}
}
